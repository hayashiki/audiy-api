type Query {
    audio(id: ID!): Audio
    audios(
        cursor: Cursor
        limit: Int
        order: [String!]
    ): AudioConnection!
    version: Version!
    comments(
        cursor: Cursor
        limit: Int
        order: [String!]
    ): AudioConnection!
}
